# Sequelize
Learn how to use Sequelize ORM with Node.js and MySQL to simplify database operations. The goal is to connect Sequelize to a MySQL database, create a table using Sequelize, and verify the table in MySQL Workbench.
